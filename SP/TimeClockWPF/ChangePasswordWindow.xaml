<Window x:Class="TimeClockWPF.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeClockWPF"
        mc:Ignorable="d"
        Title="ChangePasswordWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="New Password" Grid.Row="0"/>
        <PasswordBox x:Name="NewPassword" Grid.Row="1" Margin="0,0,0,10"/>

        <Label Content="Confirm Password" Grid.Row="2"/>
        <PasswordBox x:Name="ConfirmPassword" Grid.Row="3" Margin="0,0,0,10"/>

        <StackPanel Orientation="Horizontal" Grid.Row="4" HorizontalAlignment="center">
            <Button x:Name="ConfirmButton" Content="Confirm Password" Margin="0,10,10,0"/>
            <Button x:Name="CancelButton" Content="Cancel" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
